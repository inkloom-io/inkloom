/**
 * Core-mode domain config stub.
 *
 * Custom domains are a platform-only feature. In core mode,
 * this renders nothing. Sites are built to dist/ locally.
 */
export function DomainConfig(_props: { projectId: string; [key: string]: unknown }) {
  return null;
}
