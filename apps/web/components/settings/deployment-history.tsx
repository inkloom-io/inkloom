/**
 * Core-mode deployment history stub.
 *
 * Managed deployments are a platform-only feature. In core mode,
 * this renders nothing. Use `inkloom build` to generate static sites.
 */
export function DeploymentHistory(_props: { projectId: string; [key: string]: unknown }) {
  return null;
}
