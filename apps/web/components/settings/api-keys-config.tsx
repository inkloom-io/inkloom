/**
 * Core-mode API keys config stub.
 *
 * API keys are a platform-only feature. In core mode, this renders nothing.
 */
export function ApiKeysConfig(_props: { scope?: string }) {
  return null;
}
