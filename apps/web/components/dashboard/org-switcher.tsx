/**
 * Core-mode org switcher stub.
 *
 * Organizations are a platform-only feature. In core mode (single-tenant),
 * there's no org to switch. Renders nothing.
 */
export function OrgSwitcher() {
  return null;
}
