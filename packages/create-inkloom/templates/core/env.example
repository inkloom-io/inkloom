# Server-side URL for the Cloudflare data Worker.
DATA_API_URL=http://127.0.0.1:8787

# Optional shared secret for a remotely deployed data Worker.
DATA_API_TOKEN=

# Optional: bypass the same-origin /api/data proxy from the browser.
# NEXT_PUBLIC_DATA_API_URL=https://inkloom-data.example.workers.dev
